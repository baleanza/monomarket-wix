Зважаючи на файлову структуру та функціонал, над яким ми працювали, ваш проєкт є критично важливим інтеграційним шаром. Нижче представлений якісний `README.md`, який детально описує проєкт, його структуру та функціонал.

-----

# Monomarket/Murkit ↔ Wix Connector

## 🌟 Огляд проєкту

Цей конектор автоматизує процеси створення замовлень, синхронізації статусів, а також генерації фідів для оновлення цін та наявності товарів. Для гнучкого мапінгу SKU та налаштування каталогів використовується Google Sheets як центральне сховище конфігурації.

## ✨ Основні функції

  * **Повна синхронізація замовлень:** Створення замовлень у Wix Stores при отриманні відповідного вебхука від Murkit.
  * **Двосторонній обмін статусами:** Підтримка повного циклу API Murkit для перевірки статусу замовлення (GET, POST Batch) та ініціації скасування (PUT).
  * **Гнучке мапінгування:** Використання Google Sheets для зв'язування кодів Monomarket з SKU товарів у Wix.
  * **Синхронізація інвентарю:** Генерація XML-фіду (Stock Feed та Offers Feed) для Monomarket на основі даних про наявність та ціни у Wix.
  * **Безпечна авторизація:** Захист ендпоінтів за допомогою Basic Authentication та використання Bearer Token для Wix API.

-----

## 🏗️ Архітектура та структура файлів

Проєкт побудований як Vercel Serverless Functions і використовує модульну структуру для розділення відповідальності:

```
/
├── api/
│   ├── monomarket-endpoint.js  # 🎯 Головний обробник замовлень Murkit (Create, Get Status, Cancel, Batch)
│   ├── monomarket-offers.js    # Генерація фіду цін (Offers Feed)
│   ├── monomarket-stock.js     # Генерація фіду наявності (Stock Feed)
│   └── *.js                    # Додаткові API-файли (наприклад, для дебагу)
├── lib/
│   ├── wixClient.js            # 🔑 Клієнт для взаємодії з Wix Stores API (замовлення, продукти, Fulfillment)
│   ├── sheetsClient.js         # Клієнт для Google Sheets API (авторизація, читання мапінгу)
│   ├── feedBuilder.js          # Логіка формування XML-фіду
│   └── *.js
└── vercel.json                 # Налаштування маршрутизації та конфігурації Vercel
```

-----

## 🛠️ Налаштування та розгортання

### 1\. Передумови

  * **Node.js** (v18+ рекомендовано)
  * **Vercel CLI**
  * **GitHub/Git**
  * **Wix Stores App:** Потрібні `WIX_ACCESS_TOKEN` та `WIX_SITE_ID`.
  * **Google Cloud:** Потрібні ключі для доступу до Google Sheets та Google Drive.

### 2\. Змінні середовища (Environment Variables)

Для коректної роботи Vercel Functions повинні бути налаштовані наступні змінні середовища (у файлі `.env` для локальної розробки та у налаштуваннях Vercel для продакшену):

| Змінна | Призначення |
| :--- | :--- |
| `WIX_ACCESS_TOKEN` | Bearer токен для авторизації у Wix API. |
| `WIX_SITE_ID` | Ідентифікатор сайту Wix. |
| `MURKIT_USER` | Логін для Basic Auth вебхуків Murkit. |
| `MURKIT_PASS` | Пароль для Basic Auth вебхуків Murkit. |
| `SHEETS_ID` | ID Google-таблиці з мапінгом товарів. |
| `GOOGLE_SERVICE_ACCOUNT_EMAIL` | Email сервісного облікового запису Google. |
| `GOOGLE_PRIVATE_KEY` | Приватний ключ сервісного облікового запису (у форматі Base64 або raw). |

### 3\. Деплой

1.  Клонуйте репозиторій:
    ```bash
    git clone [repo_url]
    cd baleanza-connect
    ```
2.  Встановіть залежності:
    ```bash
    npm install
    ```
3.  Розгорніть на Vercel за допомогою CLI або через інтеграцію GitHub.

-----

## 🔗 Ендпоінти Murkit/Monomarket API

Всі маршрути Murkit API обробляються єдиним файлом `api/monomarket-endpoint.js`, відповідно до конфігурації у `vercel.json`.

| Функція | Метод | URL (згідно з `vercel.json`) | Обробник |
| :--- | :--- | :--- | :--- |
| **Створення замовлення** | `POST` | `/monomarket-endpoint` | `monomarket-endpoint.js` |
| **Отримання статусу (одне)** | `GET` | `/monomarket-endpoint/orders/{id}` | `monomarket-endpoint.js` |
| **Отримання статусу (пакет)** | `POST` | `/monomarket-endpoint/orders/batch` | `monomarket-endpoint.js` |
| **Скасування замовлення** | `PUT` | `/monomarket-endpoint/orders/{id}/cancel` | `monomarket-endpoint.js` |
| **Фід цін (Offers Feed)** | `GET` | `/monomarket-offers` | `monomarket-offers.js` |
| **Фід наявності (Stock Feed)** | `GET` | `/monomarket-stock` | `monomarket-stock.js` |

> **Примітка:** `{id}` у запитах GET та PUT — це **Wix Order ID**, який ви повертаєте Murkit після успішного створення замовлення.

-----

## 💡 Поради для розробника

  * **Розширюваність:** Якщо Murkit додасть нові типи доставки, оновіть константи `MURKIT_TO_WIX_CREATION_MAPPING` та `WIX_TO_MURKIT_STATUS_MAPPING` у файлі `api/monomarket-endpoint.js`.
  * **Відлагодження:** Використовуйте файли `api/debug-wix.js` та `api/debug-order.js` для швидкої перевірки авторизації та пошуку замовлень.
  * **Google Sheets:** Переконайтеся, що ваш сервісний обліковий запис має права лише для **читання** таблиць з мапінгом.
  * **Тестування:** Використовуйте Jest (`tests/` папка) для перевірки коректності логіки мапінгу та формування фіду.

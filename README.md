# Monomarket ↔ Wix Connector

## 🌟 Огляд проєкту

Цей конектор автоматизує процеси створення замовлень, синхронізації статусів, а також генерації фідів для оновлення цін та наявності товарів. Для гнучкого мапінгу SKU та налаштування каталогів використовується Google Sheets як центральне сховище конфігурації.

## ✨ Основні функції

  * **Повна синхронізація замовлень:** Створення замовлень у Wix Stores при отриманні відповідного вебхука від Murkit.
  * **Надійний обмін статусами:** Підтримка повного циклу API Murkit, включаючи перевірку статусу (GET Single/POST Batch) та ініціації скасування (PUT).
  * **Гнучке мапінгування:** Використання Google Sheets для зв'язування кодів Monomarket з SKU товарів у Wix.
  * **Синхронізація інвентарю:** Генерація XML-фіду (Stock Feed та Offers Feed) для Monomarket на основі даних про наявність та ціни у Wix.

-----

## 🏗️ Архітектура та структура файлів

Проєкт побудований як Vercel Serverless Functions і використовує модульну структуру для розділення відповідальності:

```
/
├── api/
│   ├── monomarket-endpoint.js  # 🎯 Головний обробник замовлень Murkit (Create, Get Status, Cancel, Batch)
│   ├── monomarket-offers.js    # Генерація фіду цін (Offers Feed)
│   ├── monomarket-stock.js     # Генерація фіду наявності (Stock Feed)
│   └── *.js
├── lib/
│   ├── wixClient.js            # 🔑 Клієнт для Wix API (замовлення, продукти, Fulfillment, Inventory)
│   ├── sheetsClient.js         # Клієнт для Google Sheets API (авторизація, читання мапінгу)
│   ├── helpers.js              # Допоміжні функції для обробки даних фіду та конвертації одиниць
│   └── *.js
└── vercel.json                 # Налаштування маршрутизації Vercel (Debug-маршрути видалені)
```

-----

## 🛠️ Налаштування та розгортання

### 1\. Змінні середовища (Environment Variables)

Для коректної роботи Vercel Functions повинні бути налаштовані наступні змінні середовища:

| Змінна | Призначення | Використання у коді |
| :--- | :--- | :--- |
| `WIX_ACCESS_TOKEN` | Bearer токен для авторизації у Wix API. | `lib/wixClient.js` |
| `WIX_SITE_ID` | Ідентифікатор сайту Wix. | `lib/wixClient.js` |
| `MURKIT_USER` | Логін для Basic Auth вебхуків Murkit. | `api/monomarket-endpoint.js` |
| `MURKIT_PASS` | Пароль для Basic Auth вебхуків Murkit. | `api/monomarket-endpoint.js` |
| **`SPREADSHEET_ID`** | ID Google-таблиці з мапінгом товарів. | `lib/sheetsClient.js` |
| **`GOOGLE_SERVICE_ACCOUNT_KEY`** | Приватний ключ сервісного облікового запису (повний JSON). | `lib/sheetsClient.js` |

> ⚠️ **Критично:** Переконайтеся, що ваш `WIX_ACCESS_TOKEN` має дозволи (Scopes), включаючи **"Wix Stores: Read Fulfillments"** та інші необхідні права для читання/запису замовлень.

### 2\. Деплой

(Інструкції щодо клонування та встановлення залежностей, як у вашому старому `README`, залишаються актуальними).

-----

## 🔗 Ендпоінти Murkit/Monomarket API

Всі маршрути Murkit API обробляються єдиним файлом `api/monomarket-endpoint.js`.

| Функція | Метод | URL (згідно з `vercel.json`) | Обробник | Примітки |
| :--- | :--- | :--- | :--- | :--- |
| **Створення замовлення** | `POST` | `/monomarket-endpoint` | `monomarket-endpoint.js` | Повертає Wix Order ID. |
| **Отримання статусу (одне)** | `GET` | `/monomarket-endpoint/orders/{id}` | `monomarket-endpoint.js` | **Використовує Batch-запит для надійної перевірки TTN.** |
| **Отримання статусу (пакет)** | `POST` | `/monomarket-endpoint/orders/batch` | `monomarket-endpoint.js` | Використовує ефективний Batch-запит Fulfillment API. |
| **Скасування замовлення** | `PUT` | `/monomarket-endpoint/orders/{id}/cancel` | `monomarket-endpoint.js` | Використовує коректний еднпоінт `/ecom/v1/orders/{id}/cancel`. |
| **Фід цін (Offers Feed)** | `GET` | `/monomarket-offers` | `monomarket-offers.js` | |
| **Фід наявності (Stock Feed)** | `GET` | `/monomarket-stock` | `monomarket-stock.js` | |

> **Примітка:** `{id}` у запитах GET та PUT — це **Wix Order ID**.

-----

## 💡 Поради для розробника

  * **Wix TTN Fix:** Проблема з отриманням TTN була вирішена шляхом використання **`getWixOrderFulfillmentsBatch`** для одиночних запитів, оскільки прямий `GET` був ненадійним.
  * **Google Sheets Data:** Проблема зі збоєм `Cannot read properties of undefined` була вирішена переходом на **послідовне читання таблиць** у `readSheetData`.
  * **Безпека:** Маршрути для налагодження (`/debug-*`) видалені з `vercel.json`.
